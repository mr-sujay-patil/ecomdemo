# --- test profile: in-memory H2 ---------------------------------------------
# Activated for every test JVM by the surefire <systemPropertyVariables> in pom.xml, so
# `./mvnw clean verify` needs no database installed or running.
#
# This file lives in src/test/resources, not src/main/resources: it is test configuration and has
# no business being packaged into the jar. Note there is deliberately no application.properties
# beside it - a file of that name here would SHADOW the main one rather than add to it.
#
# MODE=PostgreSQL makes H2 parse PostgreSQL's dialect and behave like it in the places that most
# often differ. It narrows the gap; it does not close it. Since Phase 7 the closing is done by the
# *IT tests, which run the same migrations and the same code against a real PostgreSQL in a
# Testcontainers container (see application-it.properties). This file stays because a suite that
# needs no Docker and finishes in seconds is worth keeping - it just no longer pretends to be
# proof about PostgreSQL.
spring.datasource.url=jdbc:h2:mem:ecomdemo;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=

# The suite runs the SAME Flyway migrations the application does, against H2, and then has
# Hibernate validate the result - exactly as in production. That makes `./mvnw clean verify` fail
# the moment a migration and the entities drift apart, instead of the application failing to start
# hours later. It is also why the migrations avoid PostgreSQL-only syntax for now.
#
# This is not the same as testing against PostgreSQL: H2 in PostgreSQL mode still parses and
# stores things its own way, and a migration it accepts can still fail on the real server. That is
# exactly what the integration tests are for - `./mvnw verify` now runs V1-V4 against a real
# PostgreSQL 18 as well, so a migration that only H2 accepts fails the build here rather than in
# production.
spring.jpa.hibernate.ddl-auto=validate

# data.sql is gone; the catalogue the tests expect comes from migration V2.

# --- Caching (Phase 13) -----------------------------------------------------
# OFF for the fast suite. @Cacheable becomes a no-op, so these tests need no Redis and no Docker -
# the promise made in Phase 7 that `./mvnw test` runs on any machine in seconds.
#
# That is not a gap being papered over. Caching behaviour is a claim about a real cache: that a
# second read skips the database, that an update evicts, that a BigDecimal survives the round
# trip through JSON. None of that can be proven against a HashMap standing in for Redis. It is
# proven in the *IT suite instead, against a real Redis in a container (see CacheApiIT).
spring.cache.type=none
